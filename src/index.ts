import {
  initializeParameters,
  forwardPropagation,
  backPropagation,
} from './model';
import Array2D from './math/Array2D';

export {
  // data structure
  Array2D,
  // model training
  initializeParameters,
  forwardPropagation,
  backPropagation,
};
